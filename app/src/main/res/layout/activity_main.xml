<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/margin_horizontal_ll_nav_button"
        android:layout_marginBottom="@dimen/margin_bottom_ll_nav_button"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:id="@+id/btnCategories"
            style="@style/StyleNavButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/margin_end_btn_category"
            android:layout_weight="1"
            android:backgroundTint="@color/btnCategories"
            android:text="@string/btn_categories" />

        <Button
            android:id="@+id/btnFavorite"
            style="@style/StyleNavButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_start_btn_favorite"
            android:layout_weight="1"
            android:backgroundTint="@color/btnFavorite"
            android:text="@string/btn_favorite"
            app:icon="@drawable/ic_favorite"
            app:iconGravity="end" />

    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>